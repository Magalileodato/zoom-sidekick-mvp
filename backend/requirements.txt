# ------------------------
# Backend
# ------------------------
fastapi==0.103.2            # Framework principal da API
uvicorn[standard]==0.23.2   # Servidor ASGI rápido para FastAPI
pydantic==2.5.1             # Validação e parsing de dados

# ------------------------
# OpenAI / Inteligência Artificial
# ------------------------
openai==1.32.0              # Integração com GPT
whisper==1.1.10             # Transcrição de áudio (STT)
torch==2.3.1                # Dependência do Whisper para processamento de modelos (CPU/Windows)

# ------------------------
# Text-to-Speech (TTS)
# ------------------------
gTTS==2.3.2                 # Conversão de texto em áudio (TTS)

# ------------------------
# Manipulação de Áudio
# ------------------------
pydub==0.25.1               # Conversão e manipulação de arquivos de áudio
ffmpeg-python==0.2.0        # Backend para processamento de áudio com FFmpeg
soundfile==0.12.1           # Leitura/escrita de arquivos WAV

# ------------------------
# Utilitários
# ------------------------
python-dotenv==1.0.1        # Carregar variáveis de ambiente de arquivos .env
loguru==0.7.0               # Logging avançado para debug e produção
requests==2.31.0            # Chamadas HTTP externas
